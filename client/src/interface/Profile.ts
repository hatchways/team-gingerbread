export interface Profile {
  firstName: string;
  lastName: string;
  description: string;
  address: string;
  phoneNumber: string;
  dateOfBirth: Date;
  available: boolean;
  availability: string;
  gender: string;
  email: string;
}
